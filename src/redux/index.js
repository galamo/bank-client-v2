import {
  accountAdded,
  getAccountsDone,
  getAccounts,
  deleteAccount,
  init,
  deleteAccountDone,
  updateAccount
} from "./actions";

export const bankActions = {
  accountAdded,
  updateAccount,
  getAccountsDone,
  getAccounts,
  deleteAccount,
  deleteAccountDone,
  init
};
